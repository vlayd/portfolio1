import 'package:flutter/material.dart';

class GetAddredd extends StatelessWidget {
  const GetAddredd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
